.messages
	each type in Object.keys(messages)
		each message in messages[type]
			div(class="alert alert-"+type) #{message}
				//- We want it to be dynamic so that we can change it on the fly
				//- so we are gonna concatenate in here the type of msg we want
